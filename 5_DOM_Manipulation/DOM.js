// DOM is a document object model
// Whenever you write any html code web browser convert it into document object model

// DOM is a tree like structure

// Dom allows programs and scripts to dynamically access and update the content, structure and style of the document

// JavaScript has powers to create dynamic html

//Change all the html elements in the page
// change all the CSS styles in the page
// Remove existing HTML elements and attribute
// Add new HTML elements and attribute
// React to all existing HTML events in the page
// Create new HTML events in the page

// So any HTML page is converted to a tree like structure