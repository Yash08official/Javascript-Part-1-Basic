function sum(num1, num2) {
  return num1 + num2;
}

const result = sum(2,3);
console.log(result);

// the values which are passed to the function in the parameters is called as arguments

// Arguments are values which are passed to the function and the variable which are declared inside the function they are called as parameters